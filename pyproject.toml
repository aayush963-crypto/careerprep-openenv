[project]
name = "careerprep-openenv"
version = "0.1.0"
description = "A simple OpenEnv benchmark for career guidance tasks"
requires-python = ">=3.10"
dependencies = [
    "fastapi",
    "uvicorn",
    "pyyaml",
    "openenv-core>=0.2.0"
]

[project.scripts]
server = "server.app:main"
